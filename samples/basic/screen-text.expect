# "Sound and pictures" — CLS, COLOR and LOCATE put a line in the middle of
# the screen. Nothing reaches the console stream when the console is the
# video card, so this one is asserted against the screen buffer itself.

console video
screen ^\s+HELLO FROM YOUR ACE\s*$
