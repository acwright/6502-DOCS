#!/usr/bin/env node
// Placeholder for the Phase 1 sample harness (see PLAN.md, Phase 1, task 3).
// Boots the emulator once, discovers samples/**/*.{bas,prg,asm}, and asserts
// each against its sibling .expect file. Until Phase 1 lands there are no
// samples to discover, so this exits successfully with a note.

console.log('verify-samples: no samples/ harness yet (see PLAN.md Phase 1)')
process.exit(0)
